[Looks like this file (autoconf related) is pulled from the local build environment, it's not a file that's committed to our source tree. As such we include whatever is on the build env host.

Reopen if this is a problem (you didn't indicate as such in your desc so I'm closing for now)]