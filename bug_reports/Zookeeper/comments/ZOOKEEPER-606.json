[this patch:

1) fixes the spaces
2) fixes the need for wndows java to have ; in classpath
3) fixes kill command on cygwin, Seems to work on unix as well (ubuntu), submitting., same as before:
1) fixed running from release build
2) uncommented-out the rm of the pid (had removed for testing, forgot to add back), note to reviewer(s): be sure to verify these on cygwin/unix & macosx, Verified (and using) on Win7+cygwin1.7
Thanks!, +1, Integrated in ZooKeeper-trunk #634 (See [http://hudson.zones.apache.org/hudson/job/ZooKeeper-trunk/634/])
    . bin scripts don't work with cygwin (phunt via breed)
]