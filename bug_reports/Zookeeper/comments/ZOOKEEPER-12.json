[ZOOKEEPER-849 provides a Path class. So instead of returning Paths as Strings one could return them as instances of Path., The details of the sourceforge issue:
"It may or may not be a good thing, but getChildren returns a list of names that have to be combined with the name of the parent in order to get paths.

At the least the documentation should say this. It is unlikely to be possible to change teh API, but getChildrenPaths might be a good addition.", Updating docs is good, it would be good to capture this for when we update the API (4.x?) or if someone were to create a new client, etc...]