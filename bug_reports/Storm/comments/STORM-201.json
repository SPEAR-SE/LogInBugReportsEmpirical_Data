[Patch is supplied. 

, When starting worker by supervisor, should use the same JVM path as supervisor, instead of depending on PATH env to find the right JVM.

It is typical there are multiple JVM installed on single machine.]