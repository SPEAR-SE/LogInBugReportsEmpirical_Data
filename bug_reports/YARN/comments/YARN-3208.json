[Ideally this could be done over the existing AM-RM connection if the IPC layer were extend to support it.  Otherwise we would need one side to shut down the connection and force the AM to reconnect to the RM and re-authenticate.]