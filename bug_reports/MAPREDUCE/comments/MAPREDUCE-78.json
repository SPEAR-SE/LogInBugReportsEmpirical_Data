[Owen,

I wonder if this was happening cause the TT connected with a different port each time, and the checks of host took the whole TT ID (including its client port) instead of just the hostname?, Fairly confident this has been fixed.]