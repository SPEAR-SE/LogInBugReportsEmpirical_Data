[This patch add an option "mapred.job.tracker.hostname" to let JobTracker have right url for JobProfile and others., Instead of using the config, can we simply make use of "InetAddress.getLocalHost().getHostName()" to get the host name when we bind InetSocketAddress to 0.0.0.0?]