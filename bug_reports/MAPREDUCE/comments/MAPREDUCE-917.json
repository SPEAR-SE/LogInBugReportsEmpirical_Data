[The rest of the methods from MAPREDUCE-372 should also be removed and a better way of supporting ChainMapper found. The offending methods are:

getIterator
getComparator
getInputCounter
getOutputCounter
, +1

This blocks MAPREDUCE-901 also., For now, let's revert MAPREDUCE-372. That needs some work in order to better support the ChainMapper., Closing this as INVALID since we've reverted MAPREDUCE-317., bq. Closing this as INVALID since we've reverted MAPREDUCE-317. 

That should have read as:
Closing this as INVALID since we've reverted MAPREDUCE-372.]