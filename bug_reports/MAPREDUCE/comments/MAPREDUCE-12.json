[Attaching patch which sets the processes group id to match that of a user.

Also, changing the seperate setegid(), setgid()to setresgid() and setuid() and seteuid() to setresuid(), Fixed as part of HADOOP-5420]