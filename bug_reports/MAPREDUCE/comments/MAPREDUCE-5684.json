[The failure happens when job.getTaskCompletionEvents(0, 2) is redirected to history server.  In the .jhist file all the failed attempts' status is FAILED., Seeing the same issue. , This patch changes the assert to accept both TIPFAILED and FAILED status. verifyFailingMapperCounters(job) only if status is TIPFAILED.]