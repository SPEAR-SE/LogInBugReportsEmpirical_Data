[[~jeagles], just to clarify, was that starting from an empty ~/.m2/repository before running the build?  (Otherwise, it could contain cached jars from prior versions or builds of other non-Hadoop projects.)]