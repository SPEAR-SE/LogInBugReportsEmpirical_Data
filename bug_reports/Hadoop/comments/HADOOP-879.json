[We should fix this for 0.10.1.  A unit test would be good to have too., Attached is a patch that changes validation to use FileSystem#exists(Path) rather than FileSystem#isFile(Path), permitting directories as inputs.  Does this suffice?  We still need a unit test..., I just committed this, with a test.]