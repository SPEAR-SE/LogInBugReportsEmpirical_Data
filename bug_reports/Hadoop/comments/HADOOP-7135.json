[the link above is a 404, any chance of updating it?, This appears to be fixed in trunk
FileUtil.copyMerge method now shows:

 dstFile = checkDest(srcDir.getName(), dstFS, dstFile, false);

    if (!srcFS.getFileStatus(srcDir).isDirectory())
      return false;
   
    OutputStream out = dstFS.create(dstFile);, Thanks a lot for the investigation, Jim. Closing this out.]