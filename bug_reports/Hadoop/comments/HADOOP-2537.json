[Yes this looks like an ANT 1.7 "feature"


A *nix system will have a null device /dev/null. Add the null device to the list of files to be touched; this will make sure the touch task doesn't fail.

A proposed patch is attached
, Here is another patch

Create a temp file in the temp directory (and this value is a JVM system property)
Touch the temp file along with other files
Delete the temp file
Attached file 2537.patch1, +1 code review.  Tested it and it works well., I just committed this.  Thanks Hrishikesh!]