[That would be great.  Unfortunately checkstyle doesn't come with any diffing tools (like findbugs does).

Another approach would be to get the warnings down to 0 through both tuning the rules and doing a mass cleanup of the source tree using an IDE.]