[Right now the "package" target is copying conf/* into the tar file (excluding conf/*.template).  

Owen, are you suggesting that the target should copy conf/*.template to the tar file after removing the .template extension from each file?, We no longer use ant.

(insert pink panther theme here, using the words "dead ant" to represent the horn section)]