[I don't think we support native libs on OS X ... at least I have never been able to build native libs on OS X., the native libs are meant to build, but nobody really does it.

I Think the concern here is that hdds isn't building without it.

I run tests like {{TestRawLocalFileSystemContract}} without any native lb, so I think it's some kind of odd setup, maybe you've got some option somehow forcing -Pnative on. A run of: {{mvn help:active-profiles}} lists which profiles are starting]