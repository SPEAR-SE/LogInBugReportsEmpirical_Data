[Patch, The patch fixes two issues:
1) Path to JAVA is not cygpath'd so cygwin cannot invoke it by a Windows path.
2) Path to JAVA is not in "", so cygwin shell expands it as multiple words if it has spaces (which is quite often the case, e.g. when Java is in c:\program files\java\jdk1.6.0_17).]