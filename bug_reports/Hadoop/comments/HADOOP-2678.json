[This is because the precommit builds exec separate ant processes for test-core and test-contrib.  The nigthly builds run the test target which calls test-core then test-contrib. Unfortunately, test-core may call <fail> which exits the ant process before test-contrib is called., No more contrib]