[
The problem line seems to be with using DistributedCache in pipes when running with LocalJobRunner(Application.java +91)
{code}
    String executable = DistributedCache.getLocalCacheFiles(conf)[0].toString();
{code}]