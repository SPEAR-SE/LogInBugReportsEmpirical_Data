[This is the XML file served up; it's been dynamically put together by merging in stuff with the main hadoop-common config, hence various constants. No obvious reason why it shouldn't be served up. 

If I could find the logs, I'd see what they were saying. , It may make sense to write the props to a buffer and then include a content-length header, make it easier for things to detect breakage en-route, proxy servers playing up, etc, etc. , For comparison, the namenode configuration as JSON. ]