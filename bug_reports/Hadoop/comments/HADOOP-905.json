[Yes, this looks like dead code, left over from a prior version.  It should be removed, as the paths are now fully-qualified in InputFormatBase.  Thanks for noticing this!, I fixed this, removing  the dead code.]