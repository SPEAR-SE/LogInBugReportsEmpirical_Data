[This is effectively a workaround until HDFS-2856 is implemented., This patch applies to branch-1-win, The previous patch inverted the sense of the check. This is for branch-1-win., Here's the trunk patch., This is no longer required, because HDFS-2856 has been implemented, providing SASL as a means to authenticate the DataNode instead of jsvc/privileged ports.  I'm resolving this as Won't Fix.]