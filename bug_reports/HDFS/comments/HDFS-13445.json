[In the following snippet, it tries to get the server principal from the URL.If we pass IP "KerberosAuthenticator.this.url.getHost()" will not resolved and it's gives "HTTP/_HOST".
{code:java}
 String servicePrincipal = KerberosUtil.getServicePrincipal("HTTP",  KerberosAuthenticator.this.url.getHost());
{code}
 , I have attached my initial patch.[^HDFS-13445.001.patch], [~RANith], thanks for working on patch, mind creating a test case?]