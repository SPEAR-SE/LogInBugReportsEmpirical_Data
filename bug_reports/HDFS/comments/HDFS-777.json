[ This Jira is duplicate of HDFS-172., To restate a comment in HDFS-172.

The file is created by the implementation of "-put" command (not as a side effect of SpaceQuotaExcceed exception).
I would think this is a bug about how "-put" (internally copyFromLocal) deals with errors.]