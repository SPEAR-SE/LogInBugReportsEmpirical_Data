[I think the solution here is that "refreshNodes" should be special-cased to send the RPC to all NNs in the cluster, instead of just the active one. Alternatively, we can improve the dfsadmin docs to indicate that you have to explicitly refresh on all NNs.]