[Likely exposing a namenode race condition. The server exception goes away with a 10 second sleep between these two lines.

{code}
stream.write(DATA_AFTER_RESTART);
stream.close();
{code}

I'll investigate. Not sure if related to HDFS-3811.]