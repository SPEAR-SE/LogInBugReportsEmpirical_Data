[We probably should add an extended HDFS file open function, which takes a pointer to a structure and the size of the structure.  That will allow us to add more parameters later., Duplicate of HDFS-9541]