[Added a call to cluster#waitActive() in waitForTempReplica(). This ensures the block pool threads (and hence FSDataset) are initialized.  Now passes the two test cases that were failing., +1 for the patch., I committed the patch.]