[Resolving as "Won't Fix." [~brandonli] figured out a workaround for this issue i.e. mount the NFS share with the 'sync' option. This prevents the NFS client from sending out of order data.

Brandon also updated the 2.7.0 docs with this recommendation.

We can revisit throttling if necessary.]