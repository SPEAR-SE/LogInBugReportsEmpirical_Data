[we can as well hadoop-core-test.jar as a dependency in src/contrib/eclipse-plugin/ivy.xml under common configuration., I meant the target which modifies Eclipse related files like .classpath. the idea was that not only latest versions of common jars need to be pulled but test jars as well., Resolving as 'Not a Problem' (anymore) as we've long since moved onto using Maven instead of ant on trunk and on the 2.x stable releases.]