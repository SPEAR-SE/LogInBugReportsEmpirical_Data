[It looks as though the JMX value only reports the number of nodes that were marked down during runtime.  As a result, the namenode web UI reports a different value than the JMX value after a restart of the NN.  This has some significant consequences for alerting.

]