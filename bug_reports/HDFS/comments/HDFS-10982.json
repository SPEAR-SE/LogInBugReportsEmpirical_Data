[The only other {{readDirect}} call in {{hdfsRead}} is fine because it does want to pass along {{ENOTSUP}}
{code:title=hdfsRead}
    if (f->flags & HDFS_FILE_SUPPORTS_DIRECT_READ) {
      return readDirect(fs, f, buffer, length);
    }
{code}]