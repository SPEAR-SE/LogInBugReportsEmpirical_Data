[While it is clear that the name node should be a bit more protective (what happens when edits get processed and that overflows?), but data loss can be prevented in the case of a single failure by having multiple name node directories listed in the config..., Dupe of HDFS-1594]