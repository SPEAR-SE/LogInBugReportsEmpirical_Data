[Came across this behaviour using the debugger yesterday. The initialisation of the brokerId and path was non deterministic. I added a latch that ensures it is initialises before it is used.

resolved in r763565]