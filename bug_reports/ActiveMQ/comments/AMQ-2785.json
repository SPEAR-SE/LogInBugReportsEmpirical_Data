[If there was an additional boolean flag, ignoreSlaves, would that do it for you., If I could set that with a java property, I suppose.

The crux of problem is that if you provide the web console with a list of jmx urls in a cluster, and the first in that list is up as a jdbc slave, the web console is useless and returns 500 errors.]