[This looks like a classpath issue.  I'd assume that the user that's starting the service doesn't have access to the required jars at the time the service is started. , No further feedback from reporter.  Issue appears as configuration problem on the user starting the broker. ]