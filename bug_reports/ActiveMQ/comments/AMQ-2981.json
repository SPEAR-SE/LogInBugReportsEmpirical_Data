[Using the following URI

discovery:(multicast://default?group=test&reconnectDelay=1000&maxReconnectAttempts=30&useExponentialBackOff=false

with reconnection params in the inner URI works. We need to change the docs for this, After a bit more investigation, it seems that this is regression of https://issues.apache.org/jira/browse/AMQ-2849, Fixed with svn revision 1076192]