[The property name cannot contain the '-' character as the characters in the name must pass the isJavaLetterOrDigit (or the more current isJavaIdentifierPart and isJavaIdentifierStart calls).  , This is working as it should, the dash value is treated as a minus as per selector syntax.  The names need to match those defined in the JMS Spec rules.  ]