[As an addition, removing /dev/random and symlinking it to /dev/urandom can be used as a workaround. I've placed this into ActiveMQ's init script. Yuck!, This is not a broker issue, can't be resolved at this level.]