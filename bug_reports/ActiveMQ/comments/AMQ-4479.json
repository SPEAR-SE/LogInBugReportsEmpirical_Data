[The patch., There's already a check for this on line 2433 of BrokerService:

{code}
        if (isUseJmx()) {
            connector = registerConnectorMBean(connector);
        }
{code}, No test case or other supporting data to investigate.]