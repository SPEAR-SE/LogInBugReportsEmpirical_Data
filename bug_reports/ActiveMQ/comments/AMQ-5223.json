[activemq-jms-pool is part of the activemq-osgi bundle. That might be the reason the artifact doesn't has OSGi metadata itself. Since this pool is generic it should be usable without ActiveMQ broker/client independently., Add the packaging bundle option.]