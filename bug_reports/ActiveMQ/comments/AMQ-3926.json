[jms.redeliveryPolicy.redeliveryDelay isn't a valid option for the policy class which is why you get an error.  Take a look at class RedeliveryPolicy in activemq-core., using a non-existent setting is causing the issue.]