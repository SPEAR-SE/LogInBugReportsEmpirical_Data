[Wow, that was some bad copy/paste work.  sorry.  Redoing the before/after code below:

Before:
       if (!msg.isPersistent() || connection.isUseAsyncSend() || txid!=null)) {
            this.connection.asyncSendPacket(msg);
        } else {
            this.connection.syncSendPacket(msg);
        }

After:
      if(!msg.isResponseRequired() && (!msg.isPersistent() || connection.isUseAsyncSend() || txid!=null)) {
            this.connection.asyncSendPacket(msg);
        } else {
            this.connection.syncSendPacket(msg);
        }
, sorry for the copy/past misfires.  i've just attached the updated source file.  My modifications are clearly marked in ActiveMQSession.send(), this is fixed in version 5 ???, Fixed by svn revision 646363]