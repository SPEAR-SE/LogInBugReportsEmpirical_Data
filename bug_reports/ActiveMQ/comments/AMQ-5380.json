[This test case shows that the prefetch size for the 2nd connect is the default one rather then the one specified in the policy. , When keepDurableSubsActive=false is set on the Broker, the reconnected subscriber is configured correctly. , Could not reproduce this on current master branch.  ]