[If you can create a test case that excludes the external dependencies and shows an actual problem with the *ActiveMQ* code we can take a look, otherwise I'd suggest contacting the third parties where the problem appears to be.  , No test available to validate any ActiveMQ client issue exists. ]