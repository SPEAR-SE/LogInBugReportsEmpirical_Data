[Also the script do not have +x for unix environments. So you would need to 
chmod +x perfharness-activemq.sh

Wonder if we should try to do this in the assembly., Also the ruby example dont have execute permission on its rb files.]