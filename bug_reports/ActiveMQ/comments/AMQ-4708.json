[yeah, it is not part of the default build for that reason. It needs an eap install, unzip jboss-eap-6.1.0.zip into ee-install/target and use '-P eap'
unfortunately the eap binaries are not in maven., Not much we can do here since the eap stuff isn't in maven.  ]