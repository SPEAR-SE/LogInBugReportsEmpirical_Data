[This was a package refactoring which didn't make it into the .jj file used by JavaCC to build the SQL parser.

Its now fixed in CVS HEAD and will be resolved for sure in 1.1 which is a few weeks away.]