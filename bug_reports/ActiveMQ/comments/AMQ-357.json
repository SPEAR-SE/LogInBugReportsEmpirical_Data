[in EJB you need to open & close your JMS resources each time which will cause your producer to be disconnected. 

To avoid this when producing you can use the PooledConnectionFactory which avoids really closing the underlying JMS connection, session, producer]