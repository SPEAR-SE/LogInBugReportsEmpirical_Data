[Looks like the root index cannot be created due to a locking situation in windows.  Also the lock() method executes after the root index is created so a proper file locking attempt is not yet reached.  but the lock() method depends on the root index being created so we are in a bit of a catch 22.

, fixed in 560352]