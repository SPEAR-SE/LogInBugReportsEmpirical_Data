[ActiveMQ already performs strict XML validation to detect incorrect configuration items before starting the broker, this is very good.

It should IMHO also strictly validate the options given via the URI syntax and that therefore escape standard schema based validation., Fixed on trunk.]