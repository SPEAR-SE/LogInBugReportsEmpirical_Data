[Could you tell us how to reproduce this issue? By "browsing the queue using JMX" do u mean using JConsole(internally uses JMX)? , JConsole would work, or you could grab the MBean (QueueView) from the server in a unit test., Could not reproduce this one.  ]