[Reclassifing this incident.  The broker should not really be trying to work around it's persistence storage being down.  If it's database is down, it's down.  Perhaps we should provided a way to shutdown the broker until the database is up.  But I would ]