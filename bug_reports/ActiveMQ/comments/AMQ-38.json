[The problem is that there is not a separate thread for dispatching for each session - but all message dispatching is driven from a thread associated with the TransportChannel, Incidentally I think there's a problem creating a Session inside the onMessage() thread as well...]