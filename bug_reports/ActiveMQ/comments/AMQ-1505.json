[Hi

I'm using "persistent" instead of "persist" and it works fine:

 $stomp->send( { destination => '/topic/foo', body => 'test message', persistent => 'true' } );

and I'm using postgres and mysql persistenceAdapter and apache-activemq-5.0-20071128.111935-4 , Similar tests using the C++ and .NET clients for durable subscriptions work fine on 5.3 and 5.4-SNAPSHOTs]