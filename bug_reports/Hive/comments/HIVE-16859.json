[Should we support a key=value format, so that this is extendable for adding additional params in future ?
Does it make sense to encode the 'settings' at a metadata level instead of per file level ?
]