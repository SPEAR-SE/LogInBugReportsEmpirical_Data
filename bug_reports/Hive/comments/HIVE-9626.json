[The further finding is because it uses ; as field separator. If we use other separator, such as |, the query can run successful. But it is still a bug since in 0.13, 0.14, it can create such table and parser data correctly., The workround found is backslash ; as follows

CREATE TABLE test_serde_rex(
name string,
sex string,
age string
)
ROW FORMAT SERDE
'org.apache.hadoop.hive.contrib.serde2.RegexSerDe'
WITH SERDEPROPERTIES(
'input.regex' = '([^\;]*)\;([^\;]*)\;([^\;]*)',
'output.format.string' = '%1$s %2$s %3$s'
)
STORED AS TEXTFILE;
]