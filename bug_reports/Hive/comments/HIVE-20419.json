[The validation might not be the slow part of this - the hash-function for this might be the culprit here., When inserting into the hash table, the hashCode of the key was = 759386122, a few operations later the hashKey has its hashCode changed by.

{code}
 vectorPartDesc.setDataTypeInfos(nextDataTypeInfoList);
{code}

The vpd is changing its hashCode after being inserted into a hashtable, causing this particular issue.]