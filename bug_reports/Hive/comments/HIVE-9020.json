[Hive now doesn't verify whether the user has access to the data while dropping an external table. It also checks now whether the user is the owner of the table before dropping it. , Hive now doesn't verify whether the user has access to the data while dropping an external table. It also checks now whether the user is the owner of the table before dropping it. , [~nntnag17] Thanks for reporting the issue and submitting the patch!
Is this issue seen when you set hive.metastore.authorization.storage.checks=true ?
, Will you be able to also include a unit test for the patch ?
, Patch looks fine to me, apart from some formatting issues (indentation and spaces around {{&&}}). I agree with Thejas that we should add a unit test for this., Changes have been requested so I'm setting the status to open.]