[The following lines remove table and column stats state. When commenting out the lines, the results of 
the q file are the same .
 
BasicStatsTask.java:
    public Object process(StatsAggregator statsAggregator) throws HiveException, MetaException {
..
      if (p.isTransactionalTable()) {
        // TODO: this should also happen on any error. Right now this task will just fail.
        StatsSetupConst.setBasicStatsState(parameters, StatsSetupConst.FALSE);
      }
, I think this jira should be addressed at HIVE-18395.]