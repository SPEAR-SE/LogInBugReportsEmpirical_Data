[Note I ran the same queries in MySQL and it returns the correct results (it returns {{1, NULL, 5}})., These queries doesn't work either:
* {{select not(2 <=> NULL);}}
* {{select not(NULL <=> NULL);}}

In Hive they both return {{NULL}}, in MySQL they return false and true, respectively., Fixed in HIVE-15517]