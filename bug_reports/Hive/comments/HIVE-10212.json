[Since the future right approach is to convert everything in seconds, we will leave like this. So we will see inconsistent behavior, converting an int to timestamp in milliseconds, and when converting back, we get different number. But it should be OK.  ]