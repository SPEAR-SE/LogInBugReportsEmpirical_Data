[[~Qiuzhuang]: is this a varchar/char column (instead of being String)?, Hi Gopal, both of txt and orc tables are string type for id column., Can you give us the schema for the two tables?, Attache is the schema for orc/txt table creation.

BTW, txt table use partition with int date type while orc table doesn't.]