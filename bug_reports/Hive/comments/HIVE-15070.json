[The value of the upgrade script filenames are being read into variable "script". So {{$name}} should be {{$script}}. Currently this is preventing the script from iterating over all the upgrade scripts. It just installs the base schema for hive-0.10.0 and never upgrades.

Attaching a patch that will fix this., [~spena] Could you please review this? I understand the HMS pre-commit build doesnt exist anymore but this is a simple fix. Thanks, LGTM +1]