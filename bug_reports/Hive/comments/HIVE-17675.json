[[~jdere] [~djaiswal] fyi, This only applies to ACID tables which sort the data using Row ID instead of sort column(s). For such tables SMB wont happen. For other tables a new insert goes into different files and SMB uses KeyValueMerger to merge data from those files.]