[Similar problem to 413 - multi-table inserts but for unions.

Treating union likeTableScan, and performing setKeyAndValueDesc at the end, Also added a couple of tests , Can you add some more comments?

What is the difference between these two initUnionPlan?
{code}
+    // If the plan for this reducer does not exist, initialize the plan
+    if (opMapTask == null) {
+      if (plan.getReducer() == null)
+        GenMapRedUtils.initUnionPlan(op, ctx);
+      else
+        GenMapRedUtils.splitPlan(op, ctx);
+    }
+    else if (plan.getReducer() == reducer)
+      GenMapRedUtils.initUnionPlan(op, ctx);
+    else {
+      GenMapRedUtils.initUnionPlan(ctx, currTask);
+      GenMapRedUtils.joinPlan(op, currTask, opMapTask, ctx);
+    }
+
{code}

javadoc:
{code}
+  public static void initUnionPlan(GenMRProcContext opProcCtx, Task<? extends Serializable> currTask) {
{code}
, Also please generate the patch for branch-0.3 - it didn't apply cleanly on branch-0.3.
, incorporated Zheng's comments, Committed to trunk. Thanks Namit!
Will you provide a patch for branch-0.3?
, Committed to branch-0.3. Thanks Namit!]