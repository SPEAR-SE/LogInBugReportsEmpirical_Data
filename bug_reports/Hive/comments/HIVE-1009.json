[patch has the following changes:
1) in genScriptPlan, the script operator's row resolver was generated using alias as internal column name. We should generate a unique internal column name and set the alias as alias in ColumnInfo. 
2) many log changes due to different explain column name changes. , +1

looks good - will merge if the tests pass, Committed. Thanks Ning]