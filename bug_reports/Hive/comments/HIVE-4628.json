[Are you talking about handleToSession in SessionManager ? There is a "synchronized(sessionMapLock) " block around access to it.
, Good catch Tejas. Looks like this is not an issue any more. I've set the appropriate status.]