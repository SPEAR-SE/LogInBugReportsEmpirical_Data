[I have seen tables in Hive schema lock up in Postgres if the Hive schema is not pre-initialized. Use the script /metastore/scripts/upgrade/postgres/hive-schema-0.10.0.postgres.sql to preinitialize the schema in postgres before starting the hive server. That should help.]