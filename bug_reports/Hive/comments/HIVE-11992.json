[If these need to be brought back, moving these tests to a separate test-shard would do the trick - since they'll run in parallel instead of blocking other tests., CliDriver tests are already parallelized, but total capacity of test runners is still limited...]