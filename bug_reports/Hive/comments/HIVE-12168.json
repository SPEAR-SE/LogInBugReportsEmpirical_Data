[Making a fix.  I already applied locally to the build machine, otherwise all the reports are mistakenly flagging failed tests as build failures, and skipping the test failure report., NO PRECOMMIT TESTS, +1, Committed to master, thanks for review.]