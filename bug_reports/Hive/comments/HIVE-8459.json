[Note that this is only happening on read side, not the write side., Changed this to minor as an extra shared lock on the table makes no semantic difference.  The lock on the partition would block any xlocks on the table anyway, and a read lock doesn't block other read locks or semi-shared locks.]