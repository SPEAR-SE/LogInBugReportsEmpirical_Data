[The Apache Infrastructure team manages the servers, so you would need to create a ticket in the INFRA project instead.  The page name mapping isn't 1-for-1 due to different namespacing conventions, so the rule wouldn't be that straightforward.
, Fair enough. opened INFRA-3744]