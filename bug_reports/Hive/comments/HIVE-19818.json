[one possible way to solve the problem is that getting the query id from the job configuration like:

HiveConf.getVar(job, HiveConf.ConfVars.HIVEQUERYID, "").trim(), attach a patch for 1.2.2, this may fixed at higher version, close it]