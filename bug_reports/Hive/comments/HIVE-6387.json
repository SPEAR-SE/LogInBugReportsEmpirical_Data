[hive.in.test is set in hive-site.xml used by the tests. Should we switch these new tests introduced in HIVE-6204 to also set the config parameter only in specific tests ?
I think that might be a better approach, ie use test specific codepath only in very restricted cases. 
What do people think ?
]