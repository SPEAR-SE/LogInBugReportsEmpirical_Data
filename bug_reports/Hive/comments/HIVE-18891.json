[Also if we run same query with and condition it is running perfectly

 

Explain select * from Table1 WHERE COALESCE(Col1, 'abc') in ( SELect col2 FROM Table2) and col2 LIKE 'abc';

OK, [~amitchauhan1203] Hive 1.2.1 doesn't support these kind of queries (subqueries with OR predicate). You need to use Hive 2.2.0 or up to execute these queries (see HIVE-18167, HIVE-15481)]