[This issue may be invalid, the only way we can get non-unique resources is via user configuration. There are no conflicts with e.g. tez resources on client side (and they are handled on server side). So won't fix]