[If YARN log aggregation is used, that takes care of compressing aggregated logs., This will still incur disk space on the daemons? Does YARN log aggregation also remove the aggregated files?, The logs are removed from the local fs when they're aggregated.]