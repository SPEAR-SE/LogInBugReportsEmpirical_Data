[Looks like a duplicate of HIVE-14027, i try the problem in HIVE-14027:

select *,isnull(b.id),isnull(b.class) from test.student_info a
left join test.student_score b
on a.id=b.id
where (b.id='' or b.id is null);

and get the right result :
a.id	a.name	a.l_date	b.id	b.class	b.score	b.l_date	_c1	_c2
4	musk	2016-10-18	NULL	NULL	NULL	NULL	true	true

]