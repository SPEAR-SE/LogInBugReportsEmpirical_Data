[Process locates the war file else Exits with 1, Actually I am thinking to enhance this but this might create some platform issues.

Paths beginning with '/' are full. Else path is relative?, Small patch for bad conf argument handling., +1

looks good, will commit if the tests pass, Committed. Thanks Edward]