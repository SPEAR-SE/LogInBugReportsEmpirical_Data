[It's good to have the fix you proposed. Can you post a patch?, +1 running tests, The test is still running, but there are a lot of diffs. Can you take a look? Examples like join_map_ppr.q, input_part10.q

You can use ' ant test -Dtestcase=TestCliDriver -Dqfile=join_map_ppr.q,input_part10.q ' to reproduce.]