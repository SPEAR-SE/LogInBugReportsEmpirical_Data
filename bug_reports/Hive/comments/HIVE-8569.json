[Results are correct. Since, you have not defined any windowing spec, you will get cumulated sum along with each row. To get running sum (which I assume what you want) you need to specify window with your over clause.
I further verified this with postgres, which gives same result as Hive.]