[Does anyone working on this issue?

If not, then I might try create a patch., The configuration is working in CLI, and in BeeLine in compatibility mode.

In BeeLine in beeline mode use the --showDbInPrompt command line option instead.

See HIVE-14123]