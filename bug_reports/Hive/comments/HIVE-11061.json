[This patch does the work in both the tbls and partitions tables to figure out if the table name has changed, and if so delete the existing rows and create new ones., Patch committed to hbase-metastore branch.]