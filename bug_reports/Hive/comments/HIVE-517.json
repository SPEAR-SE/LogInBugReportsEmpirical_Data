[would your problem be solved if hive wrote only data to stdout and the rest to stderr?, there's a bug outstanding (would have to find the number) to fix the logging in the local mode. once that is fixed - this problem will go away.

if this is urgent i can take a stab at it ..]