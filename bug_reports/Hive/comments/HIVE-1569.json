[local mode and miniMR are using different filesystems, so there is no single script path that work for both., can u look at scriptfile1.q

it works with add file and refers to script by name. it works for both localmode and minimr]